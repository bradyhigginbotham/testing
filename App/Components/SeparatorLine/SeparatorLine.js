import React from 'react';
import { View } from 'react-native';
import styles from './styles';

const SeparatorLine = style => <View style={[styles.separator, style]} />;

export default SeparatorLine;
